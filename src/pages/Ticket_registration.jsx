import Layout from "../component/Layout";
import Register_ticket from "../component/Register_ticket";

const Ticket_registration = () => {
	return <Layout MainComponent={Register_ticket} />;
};

export default Ticket_registration;
