import Layout from "../component/Layout"
import MainDashboard from "../component/MainDashboard"
const Dashboard = () => {
  return <Layout MainComponent={MainDashboard} />;
}

export default Dashboard